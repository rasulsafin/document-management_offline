<UserControl x:Class="DocumentManagement.Contols.ProjectControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DocumentManagement.Contols"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <GridSplitter Grid.Column="1" Width="3"/>

        <StackPanel Grid.Column="2" Orientation="Vertical">
            <WrapPanel>
                <Button Command="{Binding LoadProjectsCommand}" Margin="3,3,0,0">Загрузить проекты на диск</Button>
                <Button Command="{Binding DownloadProjectsCommand}" Margin="3,3,0,0">Скачать проекты с диска</Button>
                <Button Command="{Binding OpenFileCommand}" Margin="3,3,0,0">Показать файл</Button>
                <Button Command="{Binding DeleteFileCommand}" Margin="3,3,0,0">Удалить файл</Button>
            </WrapPanel>
            <Separator Margin="3"/>
            <WrapPanel>
                <Button Command="{Binding CreateCommand}" Margin="3,3,0,0">Создать проект</Button>
                <Button Command="{Binding DeleteCommand}" Margin="3,3,0,0">Удалить проект</Button>
                <Button Command="{Binding RenameCommand}" Margin="3,3,0,0">Переименовать проект</Button>
            </WrapPanel>
        </StackPanel>

        <ListBox Grid.Column="0" ItemsSource="{Binding Projects, UpdateSourceTrigger=PropertyChanged}" 
                 SelectedValue="{Binding SelectProject, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ID["/>
                            <TextBlock Text="{Binding ID}"/>
                            <TextBlock Text="] "/>
                            <TextBlock Text="{Binding Title}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            
        </ListBox>
    </Grid>
</UserControl>
