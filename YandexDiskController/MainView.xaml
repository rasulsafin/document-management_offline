<Window x:Class="DocumentManagement.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocumentManagement"
        mc:Ignorable="d"
        Title="Yandex Test" Height="450" Width="800">
    <Grid>
        <TabControl>
            <TabItem Header="Проекты">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="30"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="1"></StackPanel>

                    <DataGrid Grid.Row="0" ItemsSource="{Binding Projects}"></DataGrid>
                </Grid>
            </TabItem>
            
        </TabControl>
    </Grid>
</Window>
