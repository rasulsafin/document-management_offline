namespace MRS.DocumentManagement.Interface.Dtos
{
    public enum DynamicFieldType
    {
        OBJECT,
        STRING,
        BOOL,
        INTEGER,
        FLOAT,
        ENUM,
        DATE,
        UNDEFINED,
    }
}
